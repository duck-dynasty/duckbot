services:
    duckbot:
        build: .
        env_file: ./duckbot/.env

    postgres:
        image: postgres
        environment:
            POSTGRES_USER: duckbot
            POSTGRES_PASSWORD: pond
        volumes:
        - ./data
